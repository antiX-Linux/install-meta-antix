FLL_DESCRIPTION="Samba and pyneighborhood"

FLL_PRE_PROCESSING=(
	"#!/bin/bash"
	"echo \"---- in preprocessing ----\""
	"# paste your code here (not necessary for normal install, only for preconfigs)"
	"echo \"---- preprocessing done----\""
)

FLL_PACKAGE_DEPMODS=(
)

FLL_PACKAGES=(
        samba
	pyneighborhood
	smbclient 
        #smbfs
        cifs-utils 
)

